#include <iostream>
#include <conio.h>
#include <windows.h>
using namespace std;

// \245\206\251\210\344\242\230\276\253\244\217\250\235\343\340\227\275\215
//   ą   ć   ę   ł   ń   ó   ś   ż   ź   Ą   Ć   Ę   Ł   Ń   Ó   Ś   Ż   Ź

void rysunek(int &x, int &y)
{
   system("cls"); //po to zeby ramka sie "nadpisywala" zamiast tworzyc kopie przy kazdej zmianie

    for(int i=0; i<(24-x)/2; i++) //wyśrodkowanie wysokości nad ramką
		cout<<endl;

	char a = 219; //znak pełnego klocka z tabeli ascii
	/* rysowanie ramki */
    for(int i=0; i<x; i++)
    {
    	for (int j=0; j<(80-y)/2; j++) //wyśrodkowanie szerokości
			cout<<" ";
        cout <<a<<a;
        for( int l=1; l<y-1; l++)
        {
            if( i>=1 & i<x-1)
                cout<<" ";
            else
                 cout<<a;
        }
        cout <<a<<a<<endl;
    }
   	for(int i=0; i<(24-x)/2; i++) //wyśrodkowanie wysokości pod ramką
		cout<<endl;
}

void gora(int &x, int &y)
{
    if (x<24)
    {
         x++;
         rysunek(x,y);
    }
}
void dol(int &x, int&y)
{
    if (x>3)
    {
        x--;
        rysunek(x,y);
    }
}
void lewo(int &x, int &y)
{
    if (y>3)
    {
        y--;
        rysunek(x,y);
    }
}
void prawo(int &x, int &y)
{
    if (y<77)
    {
        y++;
        rysunek(x,y);
    }
}

int main()
{
	int y=3, x=3, klawisz;  //x-wysokość   y-szerokość

    cout<<"U\276ywaj strza\210ek do zmiany rozmiaru ramki"<<endl;
    Sleep(1000);
    system ("cls");
		while (true)
        {
            klawisz = getch();
            if (klawisz == 224) klawisz+=getch();
            if (klawisz == 296) gora(x,y);
            if (klawisz == 304) dol(x,y);
            if (klawisz == 301) prawo(x,y);
            if (klawisz == 299) lewo(x,y);
        }

	return 0;
}
